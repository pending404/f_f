<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Process Pending</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        html, body {
            width: 100%;
            height: 100%;
            overflow-x: hidden;
        }
        
        body {
            background-color: #0a0a0a;
            color: #fff;
            background-image: url('https://wallpapercave.com/wp/wp6277399.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            position: relative;
            min-height: 100vh;
            padding: 0;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            z-index: -1;
        }
        
        .container {
            width: 100%;
            max-width: 100%;
            margin: 0;
            padding: 20px;
            background-color: rgba(20, 20, 20, 0.8);
            animation: fadeIn 1s ease-in-out;
            position: relative;
            z-index: 1;
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
        }
        
        .logo {
            width: auto;
            max-width: 150px;
            height: auto;
            animation: glowLogoRed 2s infinite alternate;
        }
        
        .logo img {
            width: 100%;
            height: auto;
            max-height: 80px;
            object-fit: contain;
        }
        
        @keyframes glowLogoRed {
            0% { filter: drop-shadow(0 0 5px rgba(255, 0, 0, 0.7)); }
            100% { filter: drop-shadow(0 0 20px rgba(255, 0, 0, 1)); }
        }
        
        .language-toggle {
            background: linear-gradient(135deg, #ff6a00, #ff9d00);
            color: #fff;
            border: none;
            padding: 10px 15px;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s ease;
            box-shadow: 0 3px 10px rgba(255, 157, 0, 0.4);
        }
        
        .language-toggle:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 157, 0, 0.6);
        }
        
        .language-toggle:active {
            transform: translateY(1px);
        }
        
        .content {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            flex: 1;
        }
        
        .text-container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto 20px;
        }
        
        h1 {
            font-size: 22px;
            color: #00cc00;
            margin-bottom: 15px;
            text-align: left;
        }
        
        p {
            font-size: 18px;
            line-height: 1.6;
            color: #ddd;
            margin-bottom: 20px;
            text-align: left;
        }
        
        .highlight {
            color: #ff9d00;
            font-weight: bold;
        }
        
        .english-text {
            display: block;
        }
        
        .hindi-text {
            display: none;
        }
        
        /* Loading image animation */
        .unlock-image {
            height: 24px;
            width: auto;
            animation: loadingYellowGlow 2s infinite;
        }
        
        @keyframes loadingYellowGlow {
            0% { opacity: 0.7; transform: scale(0.95); filter: drop-shadow(0 0 5px rgba(255, 255, 0, 0.7)); }
            50% { opacity: 1; transform: scale(1.05); filter: drop-shadow(0 0 15px rgba(255, 255, 0, 1)); }
            100% { opacity: 0.7; transform: scale(0.95); filter: drop-shadow(0 0 5px rgba(255, 255, 0, 0.7)); }
        }
        
        .countdown-container {
            background-color: rgba(0, 0, 0, 0.5);
            padding: 20px;
            margin: 30px 0;
            text-align: center;
            width: 100%;
        }
        
        .countdown-title {
            font-size: 1.3rem;
            color: #ff9d00;
            margin-bottom: 10px;
        }
        
        .countdown-timer {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 15px;
        }
        
        .countdown-box {
            background: linear-gradient(135deg, #333, #222);
            padding: 10px;
            min-width: 70px;
        }
        
        .countdown-value {
            font-size: 1.8rem;
            font-weight: bold;
            color: #ff9d00;
        }
        
        .countdown-label {
            font-size: 0.8rem;
            color: #ddd;
            margin-top: 5px;
        }
        
        /* Man image with even slower green glow */
        .man-image-container {
            margin-top: 10px;
            position: relative;
            display: inline-block;
            width: 100%;
            text-align: center;
        }
        
        /* Loading image above man.gif with yellow glow */
        .bottom-loading-container {
            margin: 20px auto 10px auto;
            text-align: center;
        }
        
        .man-image {
            max-width: 100%;
            height: auto;
            animation: verySlowGlowingEffect 8s infinite alternate;
        }
        
        @keyframes verySlowGlowingEffect {
            0% { filter: drop-shadow(0 0 5px rgba(0, 204, 0, 0.5)); }
            100% { filter: drop-shadow(0 0 20px rgba(0, 204, 0, 0.9)); }
        }
        
        @media (max-width: 800px) {
            .container {
                padding: 15px;
            }
            
            .content {
                flex-direction: column;
                text-align: left;
            }
            
            .text-container {
                margin-right: 0;
                margin-bottom: 20px;
            }
            
            .header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .language-toggle {
                margin-top: 15px;
                align-self: flex-end;
            }
            
            h1 {
                font-size: 20px;
            }
            
            p {
                font-size: 16px;
            }
            
            .countdown-timer {
                flex-wrap: wrap;
            }
            
            .countdown-box {
                min-width: 60px;
            }
            
            .countdown-value {
                font-size: 1.5rem;
            }
            
            .man-image {
                width: 100%;
                max-width: 300px;
            }
        }
        
        @media (min-width: 801px) {
            .man-image {
                max-width: 500px;
                height: auto;
            }
            
            .content {
                justify-content: space-between;
                min-height: calc(100vh - 200px);
            }
            
            .countdown-container {
                margin-top: auto;
                margin-bottom: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">
                <img src="grna_logo.webp" alt="FF Logo">
            </div>
            <button class="language-toggle" id="languageToggle">Translate to Hindi</button>
        </div>
        
        <div class="content">
            <div class="text-container">
                <div class="english-text" id="englishText">
                    <h1>Dear Player,</h1>
                    <p>We have initiated the process to unban your Free Fire account. Everything has been set up, and your account will be unbanned soon. Normally, it happens instantly, but in your case, it might take some time. You will need to wait for 24 to 48 hours, after which your account will be unbanned.
                    If, for any reason, it is still not unbanned, then it will definitely be unbanned within 7 days—this is our promise. And if it still doesn't get unbanned, you will receive a full refund of the amount you paid. So, don't worry about it! Thank you for being a valued part of the Free Fire community!</p>
                </div>
                
                <div class="hindi-text" id="hindiText">
                    <h1>Dear Player,</h1>
                    <p>Humne tumhare Free Fire account ko unban karne ki process shuru kar di hai. Sab kuch set ho chuka hai, aur tumhara account jaldi hi unban ho jaayega. Aam taur par yeh turant ho jaata hai, lekin tumhare case mein thoda samay lag sakta hai. Tumhe 24 se 48 ghante tak intezaar karna padega, uske baad tumhara account unban ho jaayega.
                    Agar kisi wajah se fir bhi unban nahi hota hai, to yeh pakka 7 din ke andar unban ho jaayega—yeh hamara vaada hai. Aur agar tab bhi nahi hota, to jitna paisa tumne diya hai, utna pura refund mil jaayega. Isliye chinta mat karo!
                    Free Fire community ka ek mahatvapurn hissa banne ke liye dhanyavad!</p>
                </div>
            </div>
        
            <div class="countdown-container">
                <div class="countdown-title">Your account will be unbanned in:</div>
                <div class="countdown-timer">
                    <div class="countdown-box">
                        <div class="countdown-value" id="days">7</div>
                        <div class="countdown-label">Days</div>
                    </div>
                    <div class="countdown-box">
                        <div class="countdown-value" id="hours">0</div>
                        <div class="countdown-label">Hours</div>
                    </div>
                    <div class="countdown-box">
                        <div class="countdown-value" id="minutes">0</div>
                        <div class="countdown-label">Minutes</div>
                    </div>
                    <div class="countdown-box">
                        <div class="countdown-value" id="seconds">0</div>
                        <div class="countdown-label">Seconds</div>
                    </div>
                </div>
            </div>
            
            <!-- Loading image above man.gif with yellow glow -->
            <div class="bottom-loading-container">
                <img class="unlock-image" src="loding.png" alt="Loading">
            </div>
            
            <!-- Man image with even slower green glow -->
            <div class="man-image-container">
                <img class="man-image" src="man.gif" alt="Man GIF">
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Language toggle functionality
            const languageToggle = document.getElementById('languageToggle');
            const englishText = document.getElementById('englishText');
            const hindiText = document.getElementById('hindiText');
            
            let isEnglish = true;
            
            languageToggle.addEventListener('click', function() {
                if (isEnglish) {
                    // Switch to Hindi
                    englishText.style.display = 'none';
                    hindiText.style.display = 'block';
                    languageToggle.textContent = 'Translate to English';
                } else {
                    // Switch to English
                    englishText.style.display = 'block';
                    hindiText.style.display = 'none';
                    languageToggle.textContent = 'Translate to Hindi';
                }
                
                isEnglish = !isEnglish;
            });
            
            // Persistent countdown functionality
            const daysElement = document.getElementById('days');
            const hoursElement = document.getElementById('hours');
            const minutesElement = document.getElementById('minutes');
            const secondsElement = document.getElementById('seconds');
            
            // Generate a unique identifier for this device/browser
            function generateDeviceId() {
                let deviceId = localStorage.getItem('ffDeviceId');
                if (!deviceId) {
                    deviceId = 'device_' + Math.random().toString(36).substring(2, 15);
                    localStorage.setItem('ffDeviceId', deviceId);
                }
                return deviceId;
            }
            
            const deviceId = generateDeviceId();
            
            // Check if countdown has already started for this device
            function getCountdownEndTime() {
                const savedEndTime = localStorage.getItem(`ffCountdownEnd_${deviceId}`);
                if (savedEndTime) {
                    return parseInt(savedEndTime);
                }
                
                // If not started yet, set end time to 7 days from now
                const endTime = Date.now() + (7 * 24 * 60 * 60 * 1000); // 7 days in milliseconds
                localStorage.setItem(`ffCountdownEnd_${deviceId}`, endTime.toString());
                return endTime;
            }
            
            const countdownEndTime = getCountdownEndTime();
            
            // Update countdown timer
            function updateCountdown() {
                const currentTime = Date.now();
                const timeRemaining = countdownEndTime - currentTime;
                
                if (timeRemaining <= 0) {
                    // Countdown finished
                    daysElement.textContent = '0';
                    hoursElement.textContent = '0';
                    minutesElement.textContent = '0';
                    secondsElement.textContent = '0';
                    return;
                }
                
                // Calculate remaining time
                const days = Math.floor(timeRemaining / (1000 * 60 * 60 * 24));
                const hours = Math.floor((timeRemaining % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);
                
                // Update the display
                daysElement.textContent = days;
                hoursElement.textContent = hours;
                minutesElement.textContent = minutes;
                secondsElement.textContent = seconds;
            }
            
            // Initial update
            updateCountdown();
            
            // Update countdown every second
            setInterval(updateCountdown, 1000);
            
            // Disable right-click
            document.addEventListener('contextmenu', function(e) {
                e.preventDefault();
                return false;
            });
        });
    </script>
</body>
</html>